apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sRequiredResources

metadata:
  name: require-resource-limits

spec:
  match:
    namespaces:
    - demo

    kinds:
    - apiGroups: ["apps"]
      kinds: ["Deployment"]