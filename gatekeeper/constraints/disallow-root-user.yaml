apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sDisallowRootUser

metadata:
  name: disallow-root-user

spec:
  match:
    namespaces:
    - demo

    kinds:
    - apiGroups: ["apps"]
      kinds: ["Deployment"]