apiVersion: v1
kind: Service

metadata:
  name: api
  namespace: demo

  labels:
    app: api
    owner: phuong

spec:
  selector:
    app: api

  ports:
  - name: http
    port: 80
    targetPort: 8080

  type: ClusterIP